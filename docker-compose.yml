name: sports-scope

x-traefik-defaults: &traefik-defaults
  traefik.enable: "true"
  traefik.docker.network: proxy

services:
  db:
    image: postgres:18.4
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: sports_scope_development
      KEYCLOAK_DB_PASSWORD: ${KEYCLOAK_DB_PASSWORD}
    volumes:
      - db_data:/var/lib/postgresql
      - ./deploy/postgres/init-keycloak.sh:/docker-entrypoint-initdb.d/init-keycloak.sh:ro
    ports:
      - "5432:5432"
    networks:
      - default
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 10

  keycloak:
    image: keycloak/keycloak:latest
    restart: unless-stopped
    entrypoint: ["/opt/keycloak/templates/docker-entrypoint.sh"]
    command: ["start-dev", "--import-realm"]
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://db:5432/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: ${KEYCLOAK_DB_PASSWORD}
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KEYCLOAK_ADMIN:-admin}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD}
      KC_HOSTNAME: https://${KEYCLOAK_HOST:-keycloak.localtest.me}
      KC_HOSTNAME_STRICT: "false"
      KC_HTTP_ENABLED: "true"
      KC_PROXY_HEADERS: xforwarded
      KC_HEALTH_ENABLED: "true"
      STRAVA_CLIENT_ID: ${STRAVA_CLIENT_ID}
      STRAVA_CLIENT_SECRET: ${STRAVA_CLIENT_SECRET}
      RAILS_KEYCLOAK_CLIENT_SECRET: ${RAILS_KEYCLOAK_CLIENT_SECRET}
      APP_HOST: https://${APP_HOST:-app.localtest.me}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
    volumes:
      - ./deploy/keycloak:/opt/keycloak/templates:ro
    networks:
      - default
      - proxy
    depends_on:
      db:
        condition: service_healthy
    labels:
      <<: *traefik-defaults
      traefik.http.routers.keycloak.rule: "Host(`${KEYCLOAK_HOST:-keycloak.localtest.me}`)"
      traefik.http.routers.keycloak.entrypoints: websecure
      traefik.http.routers.keycloak.tls: "true"
      traefik.http.services.keycloak.loadbalancer.server.port: "8080"

  rails:
    build:
      context: .
      dockerfile: Dockerfile
    image: sports-scope-app
    restart: unless-stopped
    entrypoint: ["docker-entrypoint-rails"]
    command: ["bundle", "exec", "rails", "server", "-b", "0.0.0.0", "-p", "3000"]
    environment:
      RAILS_ENV: development
      DATABASE_HOST: db
      DATABASE_PORT: 5432
      DATABASE_USER: ${POSTGRES_USER:-postgres}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      VITE_RUBY_HOST: ${VITE_HOST:-vite.localtest.me}
      VITE_RUBY_PORT: 443
      VITE_RUBY_HTTPS: "true"
      KEYCLOAK_BASE_URL: https://${KEYCLOAK_HOST:-keycloak.localtest.me}
      KEYCLOAK_REALM: sports-scope
      KEYCLOAK_CLIENT_ID: rails-app
      RAILS_KEYCLOAK_CLIENT_SECRET: ${RAILS_KEYCLOAK_CLIENT_SECRET}
      APP_HOST: https://${APP_HOST:-app.localtest.me}
      STRAVA_CLIENT_ID: ${STRAVA_CLIENT_ID}
      STRAVA_CLIENT_SECRET: ${STRAVA_CLIENT_SECRET}
      VITE_PUBLIC_HOST: ${VITE_HOST:-vite.localtest.me}
      EXTRA_CA_CERT: /etc/ssl/extra/traefik.crt
    volumes:
      - .:/app
      - bundle_cache:/usr/local/bundle
      - node_modules_cache:/app/node_modules
      - /home/user/traefik-docker/certs/cert.pem:/etc/ssl/extra/traefik.crt:ro
    extra_hosts:
      - "${KEYCLOAK_HOST:-keycloak.localtest.me}:host-gateway"
      - "${VITE_HOST:-vite.localtest.me}:host-gateway"
      - "${APP_HOST:-app.localtest.me}:host-gateway"
    networks:
      - default
      - proxy
    depends_on:
      db:
        condition: service_healthy
      keycloak:
        condition: service_started
      vite:
        condition: service_started
      brouter:
        condition: service_healthy
    labels:
      <<: *traefik-defaults
      traefik.http.routers.rails.rule: "Host(`${APP_HOST:-app.localtest.me}`)"
      traefik.http.routers.rails.entrypoints: websecure
      traefik.http.routers.rails.tls: "true"
      traefik.http.services.rails.loadbalancer.server.port: "3000"

  brouter:
    build:
      context: ./deploy/brouter
    image: sports-scope-brouter:1.7.10
    restart: unless-stopped
    environment:
      BROUTER_XMX: ${BROUTER_XMX:-1500m}
      BROUTER_THREADS: ${BROUTER_THREADS:-4}
    volumes:
      - brouter_segments:/segments4
      - brouter_profiles:/profiles2
    networks:
      - default
      - proxy
    depends_on:
      # Le moteur n'est démarré qu'une fois les données exploitables (au moins une
      # tuile + lookups.dat). Bloquant uniquement au tout premier boot : le marqueur
      # vit dans le volume.
      brouter-sync:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:17777/robots.txt"]
      interval: 30s
      timeout: 5s
      retries: 3
    labels:
      <<: *traefik-defaults
      # Servi sous le domaine de l'app : même origine, donc pas de CORS ni de
      # certificat supplémentaire. La priorité explicite passe devant le router
      # `rails` (Host seul). Method(GET) neutralise l'endpoint d'upload de profil
      # (POST /brouter/profile) exposé par RouteServer — l'app ne fait que des GET.
      traefik.http.routers.brouter.rule: "Host(`${APP_HOST:-app.localtest.me}`) && PathPrefix(`/brouter`) && Method(`GET`)"
      traefik.http.routers.brouter.priority: "100"
      traefik.http.routers.brouter.entrypoints: websecure
      traefik.http.routers.brouter.tls: "true"
      traefik.http.services.brouter.loadbalancer.server.port: "17777"

  # Bootstrap + rafraîchissement des données de routage (tuiles .rd5, profils,
  # lookups.dat). Premier démarrage : ~3,4 Go pour l'Europe.
  brouter-sync:
    image: alpine:3.20
    restart: unless-stopped
    environment:
      BROUTER_BBOX: ${BROUTER_BBOX:--30,30,45,70}
      BROUTER_SYNC_INTERVAL: ${BROUTER_SYNC_INTERVAL:-604800}
    volumes:
      - brouter_segments:/segments4
      - brouter_profiles:/profiles2
      - ./deploy/brouter/sync.sh:/sync.sh:ro
      - ./deploy/brouter/profiles:/profiles-overlay:ro
    entrypoint:
      - /bin/sh
      - -c
      - >
        apk add --no-cache bash curl coreutils >/dev/null &&
        while true; do bash /sync.sh; sleep "$$BROUTER_SYNC_INTERVAL"; done
    healthcheck:
      # « Sain » = il y a des données à servir. start_period couvre le premier
      # téléchargement (~3,4 Go) sans que les échecs comptent comme des retries.
      test: ["CMD-SHELL", "test -f /segments4/.sync-complete"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 7200s
    networks:
      - default

  vite:
    image: sports-scope-app
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    entrypoint: ["docker-entrypoint-vite"]
    command: ["pnpm", "vite", "dev"]
    environment:
      RAILS_ENV: development
      VITE_RUBY_HOST: 0.0.0.0
      VITE_RUBY_PORT: 3036
      VITE_PUBLIC_HOST: ${VITE_HOST:-vite.localtest.me}
    volumes:
      - .:/app
      - bundle_cache:/usr/local/bundle
      - node_modules_cache:/app/node_modules
    networks:
      - default
      - proxy
    labels:
      <<: *traefik-defaults
      traefik.http.routers.vite.rule: "Host(`${VITE_HOST:-vite.localtest.me}`)"
      traefik.http.routers.vite.entrypoints: websecure
      traefik.http.routers.vite.tls: "true"
      traefik.http.services.vite.loadbalancer.server.port: "3036"

volumes:
  db_data:
  bundle_cache:
  node_modules_cache:
  brouter_segments:
  brouter_profiles:

networks:
  default:
  proxy:
    external: true
